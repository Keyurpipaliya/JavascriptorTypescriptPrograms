// Wrlte an program of Positive and Negative Numbers.

let num;

num = prompt(`Enter is Positive and Negative Number `);

if(num%2 == 0) {
    console.log(`Enter is Positive Numbers : ${num1}`);
} else {
    console.log(`Enter is  Negative Numbers : ${num2}`);
}