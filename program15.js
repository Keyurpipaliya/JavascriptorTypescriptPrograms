// write an program of interchage between two swapping numbers.

let First;
let second;

// Two numbers between Swap Numbers
swap = First;
First = second;
second = swap;

let num = prompt(`Enter is Interchange between two Swap Numbers: `);

console.log(`Enter is Two Swap Numbers: ${First}, ${second}`);
