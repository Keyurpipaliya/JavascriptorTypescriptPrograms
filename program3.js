// write an program of leap Year and Not Leap Year.

let Year;

let Age = prompt("Enter is Leap or Not Leap Year : ");

if(Year%2 == 0) {
    console.log(`Enter is Leap Year : `);
} else {
    console.log(`Enter is not Leap Year : `);
}   